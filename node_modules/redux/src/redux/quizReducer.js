// src/redux/quizReducer.js

const initialState = {
    questions: [],
    score: 0,
    // other quiz-related state
  };
  
  const quizReducer = (state = initialState, action) => {
    switch (action.type) {
      // handle quiz actions
      default:
        return state;
    }
  };
  
  export default quizReducer;
  